---
phase: 09-events-dashboard
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - dashboard/src/types/index.ts
  - dashboard/src/components/TypeBadge.tsx
  - dashboard/src/components/PRCommentSection.tsx
  - dashboard/src/pages/WorkflowsPage.tsx
  - dashboard/src/pages/WorkflowDetailPage.tsx
  - dashboard/src/hooks/useWebSocket.ts
  - dashboard/src/store/workflowStore.ts
autonomous: true
requirements: [DASH-01, DASH-02, DASH-03, DASH-04]

must_haves:
  truths:
    - "PR auto-fix workflows show a PR Fix type badge in the workflow list"
    - "Workflow list has tab filtering: All, Implementation, Review, PR Fix"
    - "PR auto-fix workflows show PR number and comment count metadata"
    - "Workflow detail page shows collapsible comment section for PR Fix workflows"
    - "Each comment shows resolution status (fixed/failed/skipped) with file:line"
    - "pr_poll_error events trigger a deduplicated toast notification"
  artifacts:
    - path: "dashboard/src/types/index.ts"
      provides: "pipeline_type on WorkflowSummary/WorkflowDetail, new EventType values, PRComment type"
      contains: "pipeline_type"
    - path: "dashboard/src/components/TypeBadge.tsx"
      provides: "Type badge component using shadcn/ui Badge"
      contains: "TypeBadge"
    - path: "dashboard/src/components/PRCommentSection.tsx"
      provides: "Collapsible comment list with summary bar and status icons"
      contains: "PRCommentSection"
    - path: "dashboard/src/pages/WorkflowsPage.tsx"
      provides: "Tab filtering and type badge integration"
      contains: "TabsTrigger"
  key_links:
    - from: "dashboard/src/pages/WorkflowsPage.tsx"
      to: "dashboard/src/components/TypeBadge.tsx"
      via: "TypeBadge rendered per workflow item"
      pattern: "TypeBadge"
    - from: "dashboard/src/pages/WorkflowDetailPage.tsx"
      to: "dashboard/src/components/PRCommentSection.tsx"
      via: "Conditional render when pipeline_type is pr_auto_fix"
      pattern: "PRCommentSection"
    - from: "dashboard/src/hooks/useWebSocket.ts"
      to: "dashboard/src/components/Toast.tsx"
      via: "pr_poll_error triggers Toast.error with deduplication"
      pattern: "pr_poll_error"
---

<objective>
Build the dashboard UI for PR auto-fix workflow visibility: type badges, tab filtering, PR metadata display, collapsible comment section with resolution status, and toast notification for poll errors.

Purpose: Users need to see PR auto-fix activity in the dashboard, distinguish it from other workflow types, drill into which comments were fixed/failed/skipped, and be notified of polling errors.
Output: Updated WorkflowsPage with tabs and badges, WorkflowDetailPage with comment section, Toast for poll errors.
</objective>

<execution_context>
@/Users/ka/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ka/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-events-dashboard/09-CONTEXT.md
@.planning/phases/09-events-dashboard/09-RESEARCH.md
@.planning/phases/09-events-dashboard/09-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 backend changes (expected shape after 09-01 completes) -->

WorkflowSummary now includes:
```typescript
pipeline_type: string | null;  // "full" | "review" | "pr_auto_fix" | null
```

WorkflowDetail now includes:
```typescript
pipeline_type: string | null;
pr_comments: PRCommentData[] | null;  // Only present for pr_auto_fix workflows
```

PRCommentData shape (from issue_cache serialization):
```typescript
interface PRCommentData {
  comment_id: number;
  file_path: string | null;
  line: number | null;
  body: string;  // Truncated to 200 chars
  author: string;
  status: 'fixed' | 'failed' | 'skipped';
  status_reason: string | null;
  html_url: string;  // GitHub comment URL
}
```

New EventType values from backend:
```
pr_comments_detected, pr_auto_fix_started, pr_auto_fix_completed,
pr_comments_resolved, pr_poll_error
```

Already in backend but missing from frontend EventType:
```
pr_fix_queued, pr_fix_diverged, pr_fix_cooldown_started,
pr_fix_cooldown_reset, pr_fix_retries_exhausted, pr_poll_rate_limited
```

From dashboard/src/components/StatusBadge.tsx: uses cva pattern for badge styling
From dashboard/src/components/ui/badge.tsx: shadcn/ui Badge component
From dashboard/src/components/ui/tabs.tsx: shadcn/ui Tabs (already used in ProfileEditModal)
From dashboard/src/components/ui/collapsible.tsx: shadcn/ui Collapsible
From dashboard/src/components/Toast.tsx: Toast.error(message) for notifications
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Frontend types, TypeBadge, tab-filtered workflow list with PR metadata</name>
  <files>dashboard/src/types/index.ts, dashboard/src/components/TypeBadge.tsx, dashboard/src/pages/WorkflowsPage.tsx, dashboard/src/store/workflowStore.ts</files>
  <action>
    1. Update `dashboard/src/types/index.ts`:
       a. Add `pipeline_type: string | null;` to WorkflowSummary interface.
       b. Add `pipeline_type: string | null;` and `pr_comments: PRCommentData[] | null;` to WorkflowDetail interface.
       c. Add PRCommentData interface: { comment_id: number; file_path: string | null; line: number | null; body: string; author: string; status: 'fixed' | 'failed' | 'skipped'; status_reason: string | null; html_url: string; }
       d. Add ALL missing EventType values to the union: 'pr_comments_detected', 'pr_auto_fix_started', 'pr_auto_fix_completed', 'pr_comments_resolved', 'pr_poll_error', 'pr_fix_queued', 'pr_fix_diverged', 'pr_fix_cooldown_started', 'pr_fix_cooldown_reset', 'pr_fix_retries_exhausted', 'pr_poll_rate_limited'.

    2. Create `dashboard/src/components/TypeBadge.tsx`:
       - Use shadcn/ui Badge component with variant="outline" per user decision (no custom components).
       - Style map: full -> blue, review -> purple, pr_auto_fix -> orange (bg-{color}-500/10, text-{color}-500, border-{color}-500/20).
       - Label map: full -> "Implementation", review -> "Review", pr_auto_fix -> "PR Fix".
       - Default to "full" when pipeline_type is null/undefined (handles legacy workflows).
       - Export: `function TypeBadge({ type }: { type: string | null })`.

    3. Update `dashboard/src/pages/WorkflowsPage.tsx`:
       a. Import shadcn/ui Tabs, TabsList, TabsTrigger from '@/components/ui/tabs'.
       b. Add state: `const [activeTab, setActiveTab] = useState('all')`.
       c. Add tab bar above the workflow list: All | Implementation | Review | PR Fix. Values: 'all', 'full', 'review', 'pr_auto_fix'.
       d. Filter workflows by pipeline_type when tab is not 'all'. Handle null pipeline_type as 'full'.
       e. Add TypeBadge next to existing StatusBadge in each workflow list item.
       f. For pr_auto_fix workflows, show PR metadata below the title: "PR #{issue_cache.pr_number} . {issue_cache.comment_count} comments". This data comes from the workflow -- check how WorkflowsPage renders items. The issue_id for PR auto-fix workflows will be "PR-{number}", and metadata may need to come from the summary. Use issue_id to display: if issue_id starts with "PR-", show it as "PR #{number}" format, and use a subtitle.

    4. Ensure workflowStore properly handles the new pipeline_type field (it should pass through from API responses -- verify Zustand store typing).
  </action>
  <verify>
    <automated>cd /Users/ka/github/existential-birds/amelia/dashboard && pnpm type-check && pnpm test:run -- --reporter=verbose && pnpm build</automated>
  </verify>
  <done>WorkflowsPage shows tab bar filtering by pipeline type. Each workflow item has a TypeBadge. PR auto-fix workflows display PR number and comment count. Frontend types include pipeline_type, PRCommentData, and all missing EventType values. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: PR comment section in workflow detail, activity log events, poll error toast</name>
  <files>dashboard/src/components/PRCommentSection.tsx, dashboard/src/pages/WorkflowDetailPage.tsx, dashboard/src/hooks/useWebSocket.ts</files>
  <action>
    1. Create `dashboard/src/components/PRCommentSection.tsx`:
       a. Import Collapsible (CollapsibleTrigger, CollapsibleContent) from '@/components/ui/collapsible'.
       b. Import CheckCircle2, XCircle, MinusCircle, ExternalLink from 'lucide-react'.
       c. Props: `{ comments: PRCommentData[] }`.
       d. Summary bar at top: count fixed/failed/skipped from the comments array. Display as "N fixed . N failed . N skipped" with colored counts (green/red/muted).
       e. Comment rows: each is a Collapsible. Trigger shows: status icon (CheckCircle2 green for fixed, XCircle red for failed, MinusCircle muted for skipped), file_path:line (or "General" if no file_path), truncated comment body (first ~80 chars), and ExternalLink icon linking to html_url (target="_blank", rel="noopener noreferrer").
       f. CollapsibleContent shows: full comment body, "by {author}" text, file:line reference, and status_reason if present (e.g., "Skipped: below aggressiveness threshold").
       g. Section header: "REVIEW COMMENTS" in uppercase muted text, matching existing section header patterns in WorkflowDetailPage.

    2. Update `dashboard/src/pages/WorkflowDetailPage.tsx`:
       a. Import PRCommentSection.
       b. Conditionally render PRCommentSection when workflow.pipeline_type === 'pr_auto_fix' and workflow.pr_comments exists and has items.
       c. Place it below the goal card section, before the activity log.
       d. Ensure existing PR orchestration events (PR_FIX_QUEUED, PR_FIX_DIVERGED, PR_FIX_COOLDOWN_STARTED, PR_FIX_COOLDOWN_RESET, PR_FIX_RETRIES_EXHAUSTED) show in the activity log -- check if the activity log already renders all events. If it filters by EventType, add pr_auto_fix_started and pr_auto_fix_completed to the displayed set. Per user decision: pr_comments_detected and pr_comments_resolved are internal (NOT shown in activity log).

    3. Update `dashboard/src/hooks/useWebSocket.ts`:
       a. Add pr_poll_error toast handling. When a WebSocket event with event_type 'pr_poll_error' arrives, call Toast.error with the event message.
       b. Add deduplication: track lastPollErrorToastMs in a module-level variable (or ref). Only show toast if more than 30 seconds since the last one.
       c. Per user decision: pr_poll_rate_limited does NOT trigger a toast.
  </action>
  <verify>
    <automated>cd /Users/ka/github/existential-birds/amelia/dashboard && pnpm type-check && pnpm test:run -- --reporter=verbose && pnpm build</automated>
  </verify>
  <done>PRCommentSection renders with summary bar and collapsible comment rows showing status icons, file:line, comment body, and external link. WorkflowDetailPage conditionally renders comment section for PR Fix workflows. PR orchestration events and pr_auto_fix_started/completed show in activity log. pr_poll_error triggers deduplicated toast. Build succeeds.</done>
</task>

</tasks>

<verification>
cd /Users/ka/github/existential-birds/amelia/dashboard && pnpm type-check && pnpm lint:fix && pnpm test:run -- --reporter=verbose && pnpm build
</verification>

<success_criteria>
- WorkflowsPage has 4-tab filter bar (All, Implementation, Review, PR Fix)
- Each workflow item shows a TypeBadge next to StatusBadge
- PR auto-fix workflows show PR number and comment count
- WorkflowDetailPage shows collapsible PR comment section for pr_auto_fix workflows
- Comment section has summary bar ("N fixed . N failed . N skipped")
- Each comment row shows status icon, file:line, body snippet, external link
- Expanded rows show full body, author, status reason
- pr_poll_error triggers deduplicated toast notification
- All frontend EventType values match backend
- pnpm build, type-check, lint, and tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/09-events-dashboard/09-02-SUMMARY.md`
</output>

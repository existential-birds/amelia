---
phase: 10-metrics-benchmarking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - amelia/server/database/migrations/009_add_pr_autofix_metrics.sql
  - amelia/server/models/metrics.py
  - amelia/server/database/metrics_repository.py
  - amelia/server/database/__init__.py
  - amelia/pipelines/pr_auto_fix/orchestrator.py
  - amelia/pipelines/pr_auto_fix/nodes.py
  - amelia/services/classifier.py
  - tests/unit/server/database/test_metrics_repository.py
  - tests/unit/pipelines/pr_auto_fix/test_metrics_collection.py
autonomous: true
requirements:
  - METR-01
  - METR-03
  - METR-05
  - METR-06

must_haves:
  truths:
    - "Every PR auto-fix pipeline run persists latency, counters, and aggressiveness to the pr_autofix_runs table"
    - "Every LLM classification decision is logged to pr_autofix_classifications with comment text snippet and prompt hash"
    - "Metrics can be queried by date range with daily bucketing and aggressiveness grouping"
  artifacts:
    - path: "amelia/server/database/migrations/009_add_pr_autofix_metrics.sql"
      provides: "pr_autofix_runs and pr_autofix_classifications tables"
      contains: "CREATE TABLE IF NOT EXISTS pr_autofix_runs"
    - path: "amelia/server/models/metrics.py"
      provides: "Pydantic response models for metrics API"
      exports: ["PRAutoFixMetricsResponse", "ClassificationsResponse"]
    - path: "amelia/server/database/metrics_repository.py"
      provides: "Database queries for metrics aggregation"
      exports: ["MetricsRepository"]
    - path: "amelia/pipelines/pr_auto_fix/orchestrator.py"
      provides: "Timing + metrics persistence in _execute_pipeline"
      contains: "time.monotonic"
  key_links:
    - from: "amelia/pipelines/pr_auto_fix/orchestrator.py"
      to: "amelia/server/database/metrics_repository.py"
      via: "MetricsRepository.save_run_metrics() call in _execute_pipeline"
      pattern: "save_run_metrics"
    - from: "amelia/pipelines/pr_auto_fix/nodes.py"
      to: "amelia/server/database/metrics_repository.py"
      via: "MetricsRepository.save_classifications() call in classify_node"
      pattern: "save_classifications"
---

<objective>
Backend data layer for PR auto-fix metrics: database tables, Pydantic models, repository queries, and metrics collection hooks in the orchestrator and classify_node.

Purpose: Provides the persistence foundation (METR-06) and data collection (METR-01, METR-03, METR-05) that the API and dashboard plans depend on.
Output: Two new DB tables, Pydantic response models, MetricsRepository, and instrumented orchestrator/classify_node.
</objective>

<execution_context>
@/Users/ka/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ka/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-metrics-benchmarking/10-CONTEXT.md
@.planning/phases/10-metrics-benchmarking/10-RESEARCH.md

@amelia/pipelines/pr_auto_fix/orchestrator.py
@amelia/pipelines/pr_auto_fix/nodes.py
@amelia/pipelines/pr_auto_fix/state.py
@amelia/services/classifier.py
@amelia/server/database/repository.py
@amelia/server/database/__init__.py
@amelia/server/models/usage.py

<interfaces>
<!-- Key types the executor needs from existing code -->

From amelia/pipelines/pr_auto_fix/state.py:
```python
class GroupFixStatus(StrEnum):
    FIXED = "fixed"
    FAILED = "failed"
    NO_CHANGES = "no_changes"

class GroupFixResult(BaseModel):
    file_path: str | None
    status: GroupFixStatus
    error: str | None = None
    comment_ids: list[int] = Field(default_factory=list)

class ResolutionResult(BaseModel):
    comment_id: int
    replied: bool = False
    resolved: bool = False
    error: str | None = None
```

From amelia/server/database/repository.py:
```python
class WorkflowRepository:
    def __init__(self, db: Database): ...
    @property
    def db(self) -> Database: ...
```

From amelia/server/database/connection.py:
```python
class Database:
    async def fetchrow(self, query: str, *args) -> asyncpg.Record | None: ...
    async def fetch(self, query: str, *args) -> list[asyncpg.Record]: ...
    async def execute(self, query: str, *args) -> str: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration, Pydantic models, and MetricsRepository</name>
  <files>
    amelia/server/database/migrations/009_add_pr_autofix_metrics.sql,
    amelia/server/models/metrics.py,
    amelia/server/database/metrics_repository.py,
    amelia/server/database/__init__.py,
    tests/unit/server/database/test_metrics_repository.py
  </files>
  <behavior>
    - save_run_metrics persists a row to pr_autofix_runs and returns the run id
    - save_classifications persists batch of classification rows linked to a run_id
    - get_metrics_summary returns PRAutoFixMetricsResponse with summary, daily buckets, and by_aggressiveness breakdown filtered by date range
    - get_metrics_summary with profile filter returns only that profile's data
    - get_metrics_summary with aggressiveness filter returns only that level's data
    - get_classifications returns paginated ClassificationsResponse filtered by date range
    - Empty date range returns zero-valued summary and empty lists (no crash)
  </behavior>
  <action>
    1. Create migration `009_add_pr_autofix_metrics.sql` with two tables exactly as specified in RESEARCH.md code example (pr_autofix_runs with UUID PK, workflow_id FK, profile_id, pr_number, aggressiveness_level, comments_processed, fixes_applied, fixes_failed, fixes_skipped, commits_pushed, threads_resolved, duration_seconds, prompt_hash, created_at; and pr_autofix_classifications with auto UUID, run_id FK, comment_id, body_snippet, category, confidence, actionable, aggressiveness_level, prompt_hash, created_at). Add indexes on created_at, profile_id, aggressiveness_level, and run_id.

    2. Create `amelia/server/models/metrics.py` with Pydantic response models: PRAutoFixMetricsSummary, PRAutoFixDailyBucket, AggressivenessBreakdown, PRAutoFixMetricsResponse, ClassificationRecord, ClassificationsResponse. Follow the exact field names from RESEARCH.md. Use `float` for fix_rate (fixed / total, 0.0 when no data).

    3. Create `amelia/server/database/metrics_repository.py` with `MetricsRepository(db: Database)`:
       - `save_run_metrics(run_id, workflow_id, profile_id, pr_number, aggressiveness_level, comments_processed, fixes_applied, fixes_failed, fixes_skipped, commits_pushed, threads_resolved, duration_seconds, prompt_hash)` -> INSERT into pr_autofix_runs
       - `save_classifications(run_id, classifications: list[dict])` -> batch INSERT into pr_autofix_classifications
       - `get_metrics_summary(start: date, end: date, profile_id: str | None, aggressiveness: str | None) -> PRAutoFixMetricsResponse` -> Three queries: (a) aggregate summary, (b) daily buckets via date_trunc('day'), (c) group by aggressiveness_level. Follow the usage.py pattern for date_trunc aggregation. Handle empty results gracefully (zero-valued summary).
       - `get_classifications(start: date, end: date, limit: int = 50, offset: int = 0) -> ClassificationsResponse` -> paginated query with total count.

    4. Export `MetricsRepository` from `amelia/server/database/__init__.py`.

    5. Write tests in `tests/unit/server/database/test_metrics_repository.py`. Mock the Database with AsyncMock. Test: save_run_metrics calls execute with correct SQL, save_classifications handles batch insert, get_metrics_summary returns correct response model structure, get_classifications returns paginated results, empty data returns zero-valued summary.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/server/database/test_metrics_repository.py -x -v</automated>
  </verify>
  <done>
    Migration file exists with both tables and indexes. Pydantic models export from metrics.py. MetricsRepository has all four methods with tests passing. Repository exported from __init__.py.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Instrument orchestrator and classify_node with metrics collection</name>
  <files>
    amelia/pipelines/pr_auto_fix/orchestrator.py,
    amelia/pipelines/pr_auto_fix/nodes.py,
    amelia/services/classifier.py,
    tests/unit/pipelines/pr_auto_fix/test_metrics_collection.py
  </files>
  <behavior>
    - _execute_pipeline measures duration_seconds using time.monotonic() and persists run metrics via MetricsRepository
    - _execute_pipeline extracts per-comment counts from group_results (iterating comment_ids, not groups) for fixed/failed/skipped
    - _execute_pipeline logs warning and continues if metrics persistence fails (does not fail the workflow)
    - classify_node persists classification decisions via MetricsRepository after LLM classification
    - classifier.py exposes get_prompt_hash() that returns SHA-256 hex digest (first 16 chars) of the normalized classification prompt
    - Metrics collection is skipped gracefully when MetricsRepository is None (backward compat)
  </behavior>
  <action>
    1. In `amelia/services/classifier.py`, add a `get_prompt_hash(aggressiveness_level: str) -> str` function that builds the classification system prompt (same way classify_comments does), normalizes whitespace with `.strip()`, and returns `hashlib.sha256(prompt.encode()).hexdigest()[:16]`. Import hashlib at top.

    2. In `amelia/pipelines/pr_auto_fix/orchestrator.py`:
       - Add `MetricsRepository` as an optional constructor param (like workflow_repo). Store as `self._metrics_repo`.
       - In `_execute_pipeline`, wrap the pipeline execution with `time.monotonic()` timing:
         ```
         start_time = time.monotonic()
         # ... existing pipeline run ...
         duration_seconds = time.monotonic() - start_time
         ```
       - After pipeline completion (inside the try block, after workflow record update), extract per-comment counters from `final_state`:
         - Iterate `group_results` and their `comment_ids` to count FIXED/FAILED per comment (use GroupFixStatus). Skipped = comments_processed - fixed - failed.
         - Count commits_pushed = 1 if commit_sha else 0
         - Count threads_resolved from resolution_results where resolved=True
       - Call `self._metrics_repo.save_run_metrics(...)` if metrics_repo is not None. Wrap in try/except, log warning on failure (Pitfall 1 from research).
       - Import `get_prompt_hash` from classifier and pass the aggressiveness level's prompt_hash.

    3. In `amelia/pipelines/pr_auto_fix/nodes.py`:
       - After `classify_comments()` returns, collect classification data for audit logging:
         For each comment + classification pair, build a dict with comment_id, body_snippet (first 200 chars of comment body), category, confidence, actionable, aggressiveness_level, prompt_hash.
       - Extract metrics_repo from config params (add to `extract_config_params` or pass via configurable). If available, call `save_classifications(run_id, classifications_data)`. Use workflow_id from config as run_id context. Wrap in try/except with warning log.
       - NOTE: The run_id for classifications needs to match the orchestrator's run. Pass a `metrics_repo` and `run_id` through the pipeline config (RunnableConfig configurable dict).

    4. Write tests in `tests/unit/pipelines/pr_auto_fix/test_metrics_collection.py`:
       - Test orchestrator timing: mock pipeline, verify save_run_metrics called with duration > 0
       - Test per-comment counting: given group_results with known comment_ids, verify correct fixed/failed/skipped counts
       - Test metrics failure isolation: mock save_run_metrics to raise, verify workflow still completes
       - Test classify_node audit: mock classify_comments, verify save_classifications called with correct data
       - Test prompt_hash: verify get_prompt_hash returns consistent 16-char hex string
  </action>
  <verify>
    <automated>uv run pytest tests/unit/pipelines/pr_auto_fix/test_metrics_collection.py -x -v</automated>
  </verify>
  <done>
    Orchestrator measures latency and persists run metrics after every pipeline execution. Classify_node logs each classification decision with prompt hash. Metrics failures are isolated (warning log, no workflow failure). All tests pass.
  </done>
</task>

</tasks>

<verification>
uv run pytest tests/unit/server/database/test_metrics_repository.py tests/unit/pipelines/pr_auto_fix/test_metrics_collection.py -x -v
uv run ruff check amelia/server/database/metrics_repository.py amelia/server/models/metrics.py amelia/pipelines/pr_auto_fix/orchestrator.py amelia/pipelines/pr_auto_fix/nodes.py amelia/services/classifier.py
uv run mypy amelia/server/database/metrics_repository.py amelia/server/models/metrics.py
</verification>

<success_criteria>
- Migration 009 exists with both tables and correct indexes
- MetricsRepository has save_run_metrics, save_classifications, get_metrics_summary, get_classifications
- Orchestrator wraps pipeline execution with time.monotonic() timing
- Per-comment counters extracted correctly (iterating comment_ids, not groups -- Pitfall 3)
- Classification audit logging captures comment_id, body_snippet, category, confidence, actionable, aggressiveness, prompt_hash
- Metrics write failures do not crash the pipeline
- All unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/10-metrics-benchmarking/10-01-SUMMARY.md`
</output>
